export const NotFound = () => {
  return (
    <div
      style={{
        minHeight: 520,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: 48,
      }}
    >
      No Data
    </div>
  );
};
